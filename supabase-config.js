window.BASKETBALL_BACKUP_CONFIG={url:'',key:'',testMode:true};
document.addEventListener('DOMContentLoaded',()=>{const simulated=new URLSearchParams(location.search).get('simulateDate'),today=new Intl.DateTimeFormat('de-DE',{dateStyle:'medium'}).format(new Date()),banner=document.createElement('aside');banner.textContent=simulated?`🧪 TESTUMGEBUNG · Keine echten Daten · Simuliertes Datum: ${simulated}`:`🧪 TESTUMGEBUNG · Keine echten Daten · Aktuelles Datum: ${today}`;banner.style.cssText='position:sticky;z-index:2000;top:0;padding:9px 12px;background:#ffd36a;color:#571018;text-align:center;font-weight:800;font-size:13px';document.body.prepend(banner);document.title='TEST · Basketball Fitness'});
